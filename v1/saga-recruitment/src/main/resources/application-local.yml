spring:
  application:
    name: passnet-recruitment-saga
  config:
    import: optional:configserver:http://localhost:9999
grpc:
  server:
    port: 6162
  client:
    recruitment-service:
      address: 'static://localhost:7072'
      enableKeepAlive: true
      keepAliveWithoutCalls: true
      negotiationType: plaintext